name: CI

on:
  schedule:
    - cron:  '0 5 * * *'

  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - name: Check weather
        env: 
          secret_email: ${{ secrets.EMAIL }}
          secret_password: ${{ secrets.PASSWORD }}
          secret_openWeatherApiKey: ${{ secrets.OPENWEATHERAPIKEY }}
        run: python3 main.py
